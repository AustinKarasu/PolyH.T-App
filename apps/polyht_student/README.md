# PolyH.T Student

Flutter student app for secure scheduled house tests, branch-assigned PDF access, attempt tracking, and exam-mode enforcement.

Build with:

```bash
flutter pub get
flutter build apk --release --dart-define=API_BASE_URL=https://backend-two-mauve-83.vercel.app/api
```
